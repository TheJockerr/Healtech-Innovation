version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: healthtech_db
      POSTGRES_USER: healthtech
      POSTGRES_PASSWORD: healthtech123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
